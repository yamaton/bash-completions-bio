# Auto-generated with h2o

_ProcessRepeats()
{
    local cur prev words cword
    _init_completion -s || return

    local word_list="  -h -help -species -lib -nolow -noint -lcambig -u -xm -ace -gff -poly -no_id -excln -orf2 -a -maskSource -x -xsmall"
    COMPREPLY=( $(compgen -W "${word_list}" -- "$cur") )
}

## -o bashdefault and -o default are fallback
complete -o bashdefault -o default -F _ProcessRepeats ProcessRepeats
