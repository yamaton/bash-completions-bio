# Auto-generated with h2o

_TransDecoderLongOrfs()
{
    local cur prev words cword
    _init_completion -s || return

    local word_list="  -t --gene_trans_map -m -G -S --output_dir -O --genetic_code --version"
    COMPREPLY=( $(compgen -W "${word_list}" -- "$cur") )
}

## -o bashdefault and -o default are fallback
complete -o bashdefault -o default -F _TransDecoderLongOrfs TransDecoder.LongOrfs
