# Auto-generated with h2o

_cpgplot()
{
    local cur prev words cword
    _init_completion -s || return

    local word_list="  -sequence -window -minlen -minoe -minpc -outfile -graph -outfeat -plot -noplot -obsexp -noobsexp -cg -nocg -pc -nopc -help"
    COMPREPLY=( $(compgen -W "${word_list}" -- "$cur") )
}

## -o bashdefault and -o default are fallback
complete -o bashdefault -o default -F _cpgplot cpgplot
