# Auto-generated with h2o

_makeblastdb()
{
    local cur prev words cword
    _init_completion -s || return

    local word_list="  -dbtype -h -help -version -in -input_type -title -parse_seqids -hash_index -mask_data -mask_id -mask_desc -gi_mask -gi_mask_name -out -blastdb_version -max_file_sz -logfile -taxid -taxid_map"
    COMPREPLY=( $(compgen -W "${word_list}" -- "$cur") )
}

## -o bashdefault and -o default are fallback
complete -o bashdefault -o default -F _makeblastdb makeblastdb
