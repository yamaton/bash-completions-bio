# Auto-generated with h2o

_unicycleralign()
{
    local cur prev words cword
    _init_completion -s || return

    local word_list="  -h --help --ref --reads --sam --contamination --scores --low_score --keep_bad --sensitivity --threads --verbosity --min_len --allowed_overlap"
    COMPREPLY=( $(compgen -W "${word_list}" -- "$cur") )
}

## -o bashdefault and -o default are fallback
complete -o bashdefault -o default -F _unicycleralign unicycler_align
